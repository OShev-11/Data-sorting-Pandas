# Data-sorting-Pandas
Первым шагом создается новый столбец в датафрейме, куда выносит год создания фильма, указанный в его названии. Если год не входит в заданный диапазон, возвращается 1900. Вторым шагом считается средний рейтинг для каждого значения в столбце years и сортируется по убыванию.
